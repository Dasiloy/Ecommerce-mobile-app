import React from 'react';
import Icon from 'react-native-vector-icons/AntDesign';
import {IconProps} from 'react-native-vector-icons/Icon';

const AntIcons = (props: IconProps) => {
  return <Icon {...props} />;
};

export default AntIcons;
